[project]
name = "vrchat-unity-skills"
version = "0.1.0"
description = "Cross-host skills for VRChat avatar workflows"
requires-python = ">=3.11"
dependencies = []

[dependency-groups]
dev = ["pyyaml>=6", "numpy>=2", "matplotlib>=3.9", "ruff>=0.9"]

[tool.uv]
package = false
